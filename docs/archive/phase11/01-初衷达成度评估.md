# 01 · 统一登录/鉴权/用户/权限/账号映射 —— 初衷达成度评估（对抗性复盘）

> 评估人：许清楚（产品经理）｜日期：2026-09-15 01:4x｜范围：已建设 5 天（Phase 0-9）的存量系统
> **评判标准：良哥 2026-09-14 口述初衷原文（见 §0）**，不是 ADR 的「全绿」结论。
> **取证纪律**：每条结论必须有实测证据（线上 HTTP / 源码 file:line / 数据库实时查询）。**无证据的一律标「未取证」，不采信 ADR 结论。**
> 本轮**只读**：未修改任何业务仓库代码。

---

## 0. 初衷原文（唯一评判标准）

| # | 初衷 |
|---|---|
| A | **统一登录、统一鉴权、用户统一管理、权限统一管理**（哪些账号有哪些系统的权限、有哪些菜单的权限）、**账号映射** |
| B | 新自研应用接入 = **接一个公共组件 + 做配置**；且自研应用**也支持独立登录**（不跳 SSO 也能登） |
| C | 建成「哪些账号有哪些系统权限、哪些菜单权限」的**可管理、可配置**系统 |
| D | **最低线 · 登录方式**：账密 + 邮箱验证码 + 忘记密码（邮箱码找回） |
| E | **最低线 · 超管**：必须有超管账号，超管邮箱 = `marschat@163.com` |
| F | 非自研系统禁止改代码 → 不接入；**自研应用只有 6 个**（portal / activecode / kb-web / cosmic-studio / kb-ops / infra-monitor） |

---

## 1. 初衷 × 现状 × 判定 对照表

| # | 初衷 | 现状（实测） | 判定 | 实测证据 |
|---|---|---|---|---|
| 1 | **统一登录**（一处登录六应用通行） | 6 应用全部注册 OIDC client 并可发起授权；IdP 会话 Cookie `Domain=marschat.online`；但**统一登录页只有「账密」，无邮箱码** | 🟡 | `GET /oauth2/authorize?client_id=marschat-kbweb&redirect_uri=https://kb.marschat.online/kb/sso-callback` → **302 → /login.html**；`/login.html` 200 且 DOM 仅 `username/password/remember-me` + 1 个「忘记密码」链接（auth-center `src/main/resources/static/login.html:48-61`） |
| 2 | **统一鉴权** | **认证统一 ✅（6/6 验签收敛）；授权（权限拦截）只有 2/6**：`@RequirePermission` 实际落点 = kb-ops 14 处 + portal 1 处（`/admin/**`）。infra 0、kb-web 后端 0、kb-gateway 0 | 🟡（偏 ❌） | `grep -rn "@RequirePermission"` → `kb-ops/src`=14、`portal/portal-server`=3（其中 2 处是注释，**真实注解仅 SsoController.java:233**）；`infra-monitor-server/src/main/resources/application.yml:25` 显式 `- com.marschat.auth.authz.AuthzAutoConfig`；`mykng/kb-gateway/.../application.yml:21` 同样排除；mykng 全仓 `grep -rln "RequirePermission\|PermissionChecker\|hasPermission"` = **0 文件** |
| 3 | **用户统一管理** | 中心 `marschat_auth.user` 活跃用户 **仅 2 个**（admin、probeuser 且 probeuser **无邮箱**）；另有 **4 套本地账号源并存**（portal `tools.sys_user` 13 行、cosmic `users` 2 行、activecode `admin_user` 1 行、infra 应急管理员） | 🟡（偏 ❌） | DB 实时：`SELECT COUNT(*) FROM user WHERE deleted=0` = **2**（总 39 行，37 行软删）；`tools.sys_user` = **13**（其中 11 行 `p9g2local/p9ga/p9ga2/p9ga3/p9g4w/p9mapw/253/p9g3u…` 是验收探针残留）；`cosmic_studio.users` = 2；`tools.admin_user` = 1 |
| 4 | **权限统一管理 · 系统级**（哪些账号有哪些系统权限） | 中心有跨应用授权矩阵 UI（portal「统一认证中心」4 页签）+ 角色 17 个 + 角色-权限绑定 133 行；**但只有 2 个活跃用户各绑 7~8 个角色，无真实生产配置** | 🟡 | DB：`sys_role` 17 行（platform 2 + client 15）；`sys_role_permission` 133；`sys_user_role` 活跃用户仅 admin(8 个角色) / probeuser(7 个角色) |
| 5 | **权限统一管理 · 菜单级**（哪些账号有哪些菜单权限） | **菜单「上报」6 应用里 5 家有**（portal 2 / kbweb 15 / kbops 16 / inframon 6 / cosmic 8；activecode 0）；但**用户级菜单减法只对 portal 一家可配**，且 `sys_user_menu_override` = **0 行** | 🟡 | DB `sys_permission GROUP BY client_id,type` 见 §2-G4 表；`menuOverrides` 全仓引用 **仅 2 处**（`portal/src/views/AdminConsoleView.vue:144` / `portal/src/views/UsersView.vue:71`）且**都硬编码 `clientId:'marschat-portal'`**；`SELECT COUNT(*) FROM sys_user_menu_override` = **0** |
| 6 | **账号映射** | 表 + 管理 UI + 上报链路有（12 行）；**登录链路只有 cosmic 1 家消费**；portal 走 `sys_user.auth_uid` JIT（不读映射表）；kb-web / kb-ops **0 行映射** | 🟡（偏 ❌） | DB `app_account_mapping` = cosmic 3（含 1 行 status=0）/ portal 7 / inframon 1 / activecode 1 / **kbweb 0、kbops 0**；登录径消费仅 cosmic `app/routers/auth.py:_lookup_center_mapping`（ADR §33.12 亦只记 cosmic 一家）；portal 侧全仓 `grep "app_account_mapping"` 只命中 `LocalAccountReporter.java:28` 的**上报**注释 |
| 7 | **公共组件 + 配置化接入** | `apps-registry.yml` + 生成器 + 四包（auth-components/frontend-common/auth-core/common-core）已建成；**Level 0 确实不改 auth-center Java**。但新应用仍需：① 各域 **nginx 加 location**（6 家各自加过）；② 前端「照抄 4 文件薄适配层」、后端「照抄三件套」；③ **auth-center 重启**（65s、清会话） | 🟡 | INTEGRATION-GUIDE.md:40-61 自称「全程不改 auth-center Java」；但同文档 §6 铁律表 **无一条提及 nginx/网关白名单**；反证：kb-web `LoginView.vue:28 authApiBase:'/kb/api/auth'`、kb-ops `'/ops/auth-api'`（并附注释「原值 /ops/ops-api 与 nginx 不一致，已改」）、portal `'/portal/auth-api'`、cosmic `'/auth-api'` —— **四家四种前缀，都是逐个改 nginx 打通** |
| 8 | **自研应用独立登录**（不跳 SSO 也能登） | 5/6 有独立登录形态；**kb-ops 明确关闭账密**（`showLocalLogin:false`，只剩邮箱码+SSO 两条路）；activecode 只有本地账密+SSO | 🟡 | `kb-ops/kb-ops-web/src/views/login/LoginView.vue:34 showLocalLogin:false`（注释：「kb-ops 后端无本地登录端点 /kb-ops/login 必 403」）；其余 4 家 + cosmic `Login.vue:36` 均 `showMailLogin:true` |
| 9 | **超管 `marschat@163.com`** | **真实存在且唯一**：`user.id=1, username=admin, role=superadmin, deleted=0, status=1`；邮箱全表唯一；已绑**全部 6 应用 admin 角色 + 平台 admin** | ✅ | DB：`SELECT id,username,email,role,status,deleted FROM user WHERE deleted=0` → 仅 2 行，其中 `1\|admin\|marschat@163.com\|superadmin\|1\|0`；`GROUP BY email` 无重复；`sys_user_role` admin 绑 role_id `1,8,36,50,64,78,92,106`（= platform admin/user + portal/cosmic/inframon/kbops/kbweb/activecode admin） |
| 10 | **登录三方式**（账密 / 邮箱码 / 忘记密码） | 端点与 UI **都有且公网可达**，但**从来没有被完整跑通过一次**：auth-center 容器（2026-09-14 09:10 启动，12,876 行全量日志）`POST /auth/forgot-password` + `/auth/reset-password` = **0 次**；`POST /auth/mail-login`（验码登录）= **0 次**，只有 1 次发码成功 | ❌（最低线未取证） | 日志：`"已生成邮箱验证码 bizType=MAIL_LOGIN, target=marschat@163.com"` + `"验证码邮件已发送至 marschat@163.com"`（各 1 次）；`grep -cE "(POST\|GET) /auth/(forgot-password\|reset-password)"` = **0**；我本人实测：`POST /auth/forgot-password {"email":"no-such-user-zzz@marschat.online"}` → **200**（端点通），`POST /auth/reset-password` → **400 验证码错误或已过期**（端点通），`POST {5 个应用代理}/mail-login` → 全部 **400 验证码错误或已过期**（链路通）。**结论：代码路径通，但真实"用码登录"与"用码重置密码"零次验证** |

### 1.1 六应用能力矩阵（实测，非 ADR 摘录）

| 应用 | SSO | 独立登录·账密 | 独立登录·邮箱码 | 忘记密码 | 权限点（menu/api） | 前端三层同源 | 后端接口闸门 | 用户管理面板 | 账号映射·上报 | 账号映射·登录径 |
|---|---|---|---|---|---|---|---|---|---|---|
| portal | ✅ BFF | ✅(本地 sys_user) | ✅ | ✅ | 2 / **1** | ✅ setupAuthGuard | ⚠️ **仅 `/admin/**` 1 处** | ✅（平台+应用双作用域） | ✅ | ❌（用 auth_uid JIT） |
| kb-web | ✅ PKCE | ✅ | ✅ | ✅ | 15 / **0** | ✅ | ❌ **0**（mykng 全仓无鉴权代码） | ✅ | ❌ 不适用 | ❌ 不适用 |
| kb-ops | ✅ PKCE | ❌ **关闭** | ✅ | ✅ | 16 / **3** | ✅ | ✅ 14 处 `@RequirePermission` | ✅ | ❌ 不适用 | ❌ 不适用 |
| infra-monitor | ✅ PKCE | ✅(应急管理员) | ✅ | ✅ | 6 / **0** | ✅ | ❌ **0**（AuthzAutoConfig 被排除） | ✅ | ✅ | ❌ |
| cosmic-studio | ✅ BFF | ✅(本地 users) | ✅ | ✅ | 8 / **0** | ❌ **无 PermissionGate/usePermissions/AuthGuard** | ⚠️ 用 **menu 点** 做写闸门（假闸门） | ❌（自建 Admin.vue） | ✅ | ✅ **唯一一家** |
| activecode | ✅ UMD | ✅(本地 admin_user) | ❌ **无** | ❌ **无** | **0 / 0** | ❌ 不适用（静态页） | ❌ 不适用 | ❌ **无面板** | ✅ | ❌ |

> 数据来源：`sys_permission` 实时 GROUP BY、`sys_app_client.last_sync_at`、各前端源码 grep、各后端 grep。

---

## 2. 真实缺口清单 G

### 🔴 P0（必须修，直接打脸初衷或最低线）

| # | 现象 | 证据 | 影响初衷 | 建议处置 |
|---|---|---|---|---|
| **G1** | **忘记密码 与 邮箱验证码登录 两条链路从未被真实执行过一次** —— 只验证过"能发码"，没验证过"能用码登录 / 能重置密码" | auth-center 容器 StartedAt=2026-09-14T09:10，全量日志 12,876 行：`(POST\|GET) /auth/(forgot-password\|reset-password)` = **0 次**；`POST /auth/mail-login`（验码）= **0 次**；`POST /auth/mail-login/send-code` = 2 次（1 次被 60s 限流、1 次成功并发出邮件） | **D（最低线）** | ① 用真实邮箱做 1 次端到端：发码→收码→邮箱码登录→成功；发码→重置密码→用新密码登录。② 把这两条路径写成可重跑的 E2E 脚本纳入交付门禁。**未做之前，D 只能判「未取证」，不能判「已达成」** |
| **G2** | **权限统一管理是「假闸门」**：认证统一了，**授权只在 2/6 应用**；且 portal 的系统凭据接口任何已登录普通用户可读可写 | ① `portal/portal-server/.../controller/PortalSystemController.java:44-65`：`GET /{id}/credentials`、`POST`、`PUT /{id}`、`DELETE /{id}` **全无 `@RequirePermission`**，只有 `WebMvcConfig.java:18-22` 的 `JwtInterceptor`（只认证）。实测无 token → 401，**有 token 的普通用户即可调用**。② `mykng` 全仓 `grep -rln "RequirePermission\|PermissionChecker\|hasPermission"` = **0 文件**（kb-web 后端零鉴权）。③ `infra-monitor-server/application.yml:25` 显式排除 `AuthzAutoConfig`，6 个 Controller 0 注解。④ `cosmic-studio/app/routers/auth.py:321/338/400/521` 用 `require_permission("menu:admin")` —— **拿 menu 权限点当接口闸门**，而 ADR §33.10.2 自己已判定"用 menu 点做闸门是假闸门"。⑤ cosmic 前端 `grep "PermissionGate\|usePermissions\|setupAuthGuard"` = **0**（中心权限下发了但前端不消费），反而自建了一套 `menu_perms` 本地字段（`Admin.vue:162-239`）——**两套菜单权限机制并存** | **A/C** | ① portal：`/api/sys/**` 全部挂 `@RequirePermission`（至少写操作 + credentials 读）。② infra：取消 `AuthzAutoConfig` 排除，先给写接口挂点。③ kb-web：kb-gateway 增加权限点校验或在 kb-server 引入 auth-core。④ cosmic：写闸门改用 `api:` 权限点，并接入 `PermissionGate/createAuthGuard`，把本地 `menu_perms` 收敛到中心。⑤ **原则：前端隐藏不算闸门，必须是后端拦** |
| **G3** | **activecode 是 6 应用里明确掉队的一个**：登录无邮箱码、无忘记密码；0 权限点 0 菜单；无用户管理面板；内嵌 UMD 版本 0.6.9 本身不含 mailLogin 能力 | ① `activecode/login.html` DOM 只有「用户名+密码+或 使用统一认证(SSO)」两个入口（`:60-71`）。② `marschat-auth-core.umd.js` `grep -c "showMailLogin\|mail-login\|mailLogin"` = **0**；版本戳 0.6.9。③ DB `sys_permission` 无 `marschat-activecode` 任何行；`sys_app_client.last_sync_at` = NULL。④ 全仓无 `UserManagementPanel` 引用 | **D / A / C / F** | 二选一（需良哥拍板）：(a) 把 UMD 升到 0.8.x 并补「邮箱码 + 忘记密码」两个入口；(b) 明确书面豁免 activecode（它只有 1 个管理员账号）。**推荐 (a)** —— 否则"最低线"在 6 应用里就是 5/6 |

### 🟡 P1

| # | 现象 | 证据 | 影响初衷 | 建议处置 |
|---|---|---|---|---|
| **G4** | **用户级菜单减法只对 portal 一家可配**，其余 4 家有菜单权限点却无法对具体用户做减法 | `menuOverrides` 全仓引用仅 2 处：`portal/src/views/AdminConsoleView.vue:144-150`（平台作用域，仍硬编码 `clientId:'marschat-portal'`）与 `portal/src/views/UsersView.vue:71`。kb-ops(16)/kb-web(15)/infra(6)/cosmic(8) 的菜单**无配置入口**；`sys_user_menu_override` = **0 行** | **C（菜单级权限可配置）** | 在 AdminConsoleView 的「统一用户」页签把 `menuOverrides.clientId` 改成**可下拉选应用**（与「角色与菜单授权」页签已有的 `APPS` 下拉同款），后端端点已就绪（ADR §33.3 实测 GET/PUT/回读/校验/可逆全通） |
| **G5** | **账号映射登录径只有 cosmic 1 家消费**，其余应用"建了台账不用" | `app_account_mapping` 12 行：portal 7 / cosmic 3 / inframon 1 / activecode 1 / **kbweb 0、kbops 0**；登录径消费仅 cosmic（`app/routers/auth.py:_lookup_center_mapping`）；portal 走 `sys_user.auth_uid` JIT，其 7 行映射对登录**无作用**；`GET /user/mapping` 公网不可达（ADR §33.12.3 已登记未修） | **A（账号映射）** | ① 明确口径：只有"有本地账号体系"的 3 家（portal/cosmic/activecode）需要登录径消费映射；kb-web/kb-ops/infra 纯中心账号，映射不适用（免做）。② portal 的 JIT 与映射表二选一，不要两套并存。③ 补 auth nginx `/user/*` 代理面（已登记） |
| **G6** | **IdP 统一登录页没有邮箱验证码登录方式** —— 走 SSO 路径的用户只能用账密 | `auth-center/src/main/resources/static/login.html:48-61`：表单仅 `username` + `password` + `remember-me` + 1 个「忘记密码」链接，无 mail-code tab；而组件 `LoginPanel.vue:15-23` 是有的 | **A/D** | 在 `/login.html` 增加「邮箱验证码登录」tab（复用 `/auth/mail-login/**`，该链已在 `SecurityConfig.java:257-266` permitAll）。否则"统一登录三方式"名不副实 |
| **G7** | **配置化接入仍未闭环**：除 registry+生成器外，每接一个新应用还要改 nginx、抄 4 个前端文件 + 3 个后端文件、重启 auth-center | INTEGRATION-GUIDE.md:40-61 只写 registry+生成器；§6「踩坑铁律」15 条**无一条关于 nginx/网关白名单**；实证：4 家 4 种 authApiBase 前缀（`/kb/api/auth`、`/ops/auth-api`、`/portal/auth-api`、`/auth-api`），kb-ops 与 infra 源码注释都记录了"原值与 nginx 不一致导致 405/404 后改"；Level 1 明确要求"照抄 4 个文件薄适配层""照抄三件套" | **B** | ① 在指南补一节「接入必改的基础设施清单」（nginx location 模板 + 环境变量 + 重启影响）。② 把「薄适配层」再抽一层：提供 `createAppAuth(config)` 工厂，让 4 个文件降到 1 个配置文件 |
| **G8** | **用户未真正统一**：中心仅 2 个活跃用户，4 套本地账号源并存，且本地表残留 11 行验收探针账号 | DB：`user where deleted=0` = 2（其中 probeuser **email=NULL**）；`tools.sys_user` = 13（11 行 `p9*` 探针残留，中心侧已软删但本地未清）；`cosmic_studio.users` = 2；`tools.admin_user` = 1 | **A（用户统一管理）** | ① 清理 portal `sys_user` 的 11 行探针残留（先备份）。② 明确"本地账号"定位：只在断网应急时用，日常全部走中心。③ 至少给 probeuser 补邮箱，或删掉 |
| **G9** | 权限点**粒度严重不足**：51 个权限点里 api 类型只有 **4 个**（kbops 3 + portal 1），5/6 应用 0 个 api 点 | DB `sys_permission` GROUP BY：portal menu2/api1、kbweb menu15、kbops menu16/api3、inframon menu6、cosmic menu8、activecode 0 | **C** | 至少给每家的**写接口**补 api 权限点（先补 infra / kb-web / cosmic 的写端点），否则"接口级权限统一管理"永远只有 2 家 |

### ⚪ P2（登记，可排期）

| # | 现象 | 证据 | 建议 |
|---|---|---|---|
| **G10** | 公网 auth 域名 `POST /auth/mail-login` 被 nginx **301** 到 `/auth/mail-login/`，而带斜杠路径返回 **404 接口不存在**；直连容器 200 | `curl -i POST https://auth.marschat.online/auth/mail-login` → `301 Location: /auth/mail-login/`；`POST /auth/mail-login/` → `{"code":404,"message":"接口不存在: /auth/mail-login/"}`；`ssh → curl POST http://127.0.0.1:8085/auth/mail-login` → **200** | 修 auth nginx location（去掉多余 301）。当前 6 应用走各自域代理绕过，**暂不影响**，但对任何直连调用方是致命的 |
| **G11** | **范围蔓延**：`sys_app_client` 与 `oauth2_registered_client` 各 10 个客户端，含 `marschat-tokenhub` / `marschat-memory` / `frp-manager` / `p3-probe-client`，均不在良哥 6 应用清单 | DB 实时查询结果 | 与良哥确认后下线多余 client（尤其 `p3-probe-client` 是探针残留）；或明确登记为"已注册未接入" |
| **G12** | kb-ops **无账密独立登录**，只剩邮箱码 + SSO；SMTP/邮件链路一旦故障就完全登不进 | `kb-ops LoginView.vue:34 showLocalLogin:false` | 保留现状（后端确无端点）或补一个应急管理员端点 |
| **G13** | 管理面板覆盖不全：`UserManagementPanel` 4 家（缺 cosmic/activecode）；`AccountMappingPanel` 只有 portal + kb-ops | 源码 grep | 至少把 `AccountMappingPanel` 补到 infra（已有 1 行映射却无 UI 管理） |
| **G14** | 验收残留角色：`sys_role` 有 `marschat-portal` 的 `g3-adm2(120)` / `g3-adm5(127)`，无权限绑定、无用户绑定 | DB | 清理（client 角色无删除端点，需补端点或手工清） |
| **G15** | 超管只有 1 个真人账号，邮箱失联即失去全部管理能力（无备用超管、无密钥兜底） | `user where deleted=0` = 2 | 增加 1 个备用超管（或给 admin 加 TOTP/恢复码） |

---

## 3. 需求完整性核查（良哥最低线 第 4 / 5 条，逐项）

### 3.1 第 4 条：登录方式必须有 —— 账密 / 邮箱验证码 / 忘记密码

| 方式 | 后端端点 | 公网可达 | 前端 UI | **真实跑通** | 判定 |
|---|---|---|---|---|---|
| 账密 | `POST /auth/login`（`AuthController.java:33`）+ Spring `POST /login`（`SecurityConfig.java:120`） | ✅ | ✅ 5/6 应用（kb-ops 关闭） | ✅ 日志中 `POST /auth/login` 有成功记录 | ✅ |
| 邮箱验证码·**发码** | `POST /auth/mail-login/send-code`（`AuthController.java:101`） | ✅ 直连 + 5 个应用代理全部可达（我用非法邮箱实测 5/5 返回 `400 参数校验失败: email: 邮箱格式不正确`，证明链路真的到了 auth-center） | ✅ `LoginPanel.vue:69-97`，5/6 应用 `showMailLogin:true`（activecode 无） | ✅ 日志「验证码邮件已发送至 marschat@163.com」（SMTP `smtp.163.com` / `marschat@163.com` 已在容器 env 配好） | ✅ |
| 邮箱验证码·**验码登录** | `POST /auth/mail-login`（`AuthController.java:110`） | ✅ 同上 5/5 可达 | ✅ 同上 | ❌ **0 次** | ❌ **未取证** |
| 忘记密码·**发码** | `POST /auth/forgot-password`（`AuthController.java:81`，`SecurityConfig.java:239` permitAll） | ✅ 页面 `/forgot-password.html` 200；接口我用不存在邮箱实测 **200**（无邮箱枚举） | ✅ `LoginPanel.vue:143-270` 三步流程 | ❌ **0 次** | ❌ **未取证** |
| 忘记密码·**重置** | `POST /auth/reset-password`（`AuthController.java:91`） | ✅ 实测 `400 验证码错误或已过期`（端点通） | ✅ | ❌ **0 次** | ❌ **未取证** |

**结论：第 4 条「做得出来」但「没验过」。** 端点、UI、SMTP 凭据、代理链路全部实测可达，**但三条真实业务路径（验码登录 / 发码找回 / 重置密码）在 auth-center 全量日志里零命中**。按"先证伪再归因"纪律，第 4 条判定 = **❌ 未达成（未取证）**，处置见 G1。

### 3.2 第 5 条：必须有超管账号，超管邮箱 = `marschat@163.com`

| 检查项 | 实测 | 判定 |
|---|---|---|
| 账号存在 | `user.id=1, username=admin, role=superadmin, status=1, deleted=0` | ✅ |
| 邮箱正确且唯一 | `email = 'marschat@163.com'`；`GROUP BY email` 在活跃用户中无重复（`uk_email` 约束已建） | ✅ |
| 不可删 | ADR §22.1 记「superadmin 不可删」；本轮只读未做删除验证 | 🟡 未取证 |
| 能管理全部 6 应用 | `sys_user_role`：admin 绑 `platform admin(1)` + `portal(36)/cosmic(50)/inframon(64)/kbops(78)/kbweb(92)/activecode(106)` 的 admin 角色 | ✅ |
| 门户闸门认超管 | `SsoController.java` 注释明确 `requireAdmin` 必须同时认 `superadmin`；`portal UsersView.vue:52-56` roles 含 `superadmin` | ✅（代码层） |
| 备用超管 / 恢复通道 | **无**；全平台仅此 1 个真人管理员账号 | ❌ 见 G15 |

**结论：第 5 条 = ✅ 达成**（唯一缺口是没有备用超管，建议项非阻塞）。

---

## 4. 待确认问题（需良哥拍板）

| # | 问题 | 选项 | 我的推荐 |
|---|---|---|---|
| Q1 | **activecode 要不要补齐「邮箱验证码 + 忘记密码」？**（当前 6 应用里唯一缺的，直接影响最低线 D 是 6/6 还是 5/6） | A. 补齐（升 UMD 到 0.8.x + 加两个入口）<br>B. 书面豁免（它只有 1 个管理员账号） | **A**。豁免会让"最低线"变成有例外的口子；且它已经是 6 应用里权限点 0、无用户面板的"掉队户" |
| Q2 | **权限闸门要做到什么程度？**（当前：后端真拦只在 kb-ops + portal `/admin/**`） | A. 全量：6 应用所有写接口 + 敏感读接口都挂 api 权限点（工作量最大）<br>B. 收敛：先补 portal `/api/sys/**`、infra 写接口、cosmic 改 api 点，共 3 处<br>C. 维持现状，接受"前端隐藏为主" | **B**。A 成本高且当前只有 2 个真实用户；C 会让"权限统一管理"停留在演示层。B 用最小成本把"假闸门"变成"真闸门" |
| Q3 | **账号映射要不要 6 应用全量打通登录径？** | A. 全量 6 家<br>B. 只做有本地账号的 3 家（portal / cosmic / activecode），kb-web / kb-ops / infra 明确"不适用" | **B**。后 3 家无本地账号体系，映射无对象；A 是纯浪费 |
| Q4 | **是否清理范围外的客户端与探针残留？**（`marschat-tokenhub` / `marschat-memory` / `frp-manager` / `p3-probe-client`；portal `sys_user` 的 11 行 `p9*` 残留） | A. 全部清理<br>B. 只清 `p3-probe-client` + `sys_user` 探针行，其余保留备用<br>C. 都不动 | **B**。tokenhub/memory/frp 可能是后续要用；探针残留是脏数据必须清 |
| Q5 | **IdP 统一登录页要不要加「邮箱验证码登录」tab？** | A. 加<br>B. 不加（应用侧已有，够了） | **A**。既然叫"统一登录"，三方式就应在统一入口齐备 |
| Q6 | **要不要现在做一次真实邮箱端到端验证？**（会向 `marschat@163.com` 发 1~2 封真实验证码邮件） | A. 做（我这边执行，良哥收邮件报码）<br>B. 不做，先补 E2E 脚本留待后续 | **A**。这是把 G1 从"未取证"变成"已达成"的唯一办法，成本就是收 2 封邮件 |
| Q7 | **kb-ops 要不要补账密独立登录？**（当前只有邮箱码 + SSO，邮件挂了就登不进） | A. 补应急管理员端点<br>B. 维持现状 | **B**（当前无真实用户，风险低），但登记为运维风险 |

---

## 5. 一句话总结

**地基（认证统一、超管、组件化、配置注册）是真的建起来了；但「统一鉴权/权限统一管理」停在"前端隐藏 + 2/6 后端真拦"的演示层，「账号映射」停在台账层，「用户统一管理」实际只有 2 个活跃账号、4 套账号源并存，而良哥亲自划的两条最低线里，第 5 条（超管）✅ 达成、第 4 条（登录三方式）**做得出但一次都没真跑过**，第 6 个应用 activecode 明确掉队。

---

### 附：本轮取证清单（可复核）

| 手段 | 对象 |
|---|---|
| 线上 HTTP | `auth.marschat.online`（`/auth/session` 200、`/oauth2/jwks` 200、`/login.html` 200、`/forgot-password.html` 200、`/oauth2/authorize` 302→/login.html、`/auth/mail-login/send-code` 400、`/auth/forgot-password` 200、`/auth/reset-password` 400、`/auth/mail-login` **301→404**、`/admin/**` 403）；6 个应用入口全部 200；5 个应用 `mail-login` / `send-code` 代理全部可达 |
| 源码 grep | devtools（4 前端 + 3 后端 + activecode UMD）、auth-center（84 个 java）、marschat-components（LoginPanel / 各面板）、cosmic-studio（frontend + app） |
| 数据库实时查询 | `marschat_auth`（user / sys_permission / sys_role / sys_role_permission / sys_user_role / sys_user_menu_override / app_account_mapping / sys_app_client / oauth2_registered_client）、`tools`（sys_user / admin_user）、`cosmic_studio`（users） |
| 容器日志 | `auth-center` 全量 12,876 行（StartedAt 2026-09-14T09:10:31Z），统计关键端点命中次数 |
| 环境核验 | 本地 devtools HEAD `022e854f` == 服务器 `/root/devtools` HEAD `022e854f`（代码同步） |
